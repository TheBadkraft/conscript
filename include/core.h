// core.h
#ifndef CORE_H
#define CORE_H

#include <stdint.h>

typedef void* object;           // Intent: reference to an object
typedef uintptr_t addr;         // Intent: address for collections
typedef char* string;           // Intent: string representation

#endif // CORE_H
